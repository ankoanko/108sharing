<%= form_with(model: review_reply, local: true) do |form| %>
  <% if review_reply.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(review_reply.errors.count, "error") %> prohibited this review_reply from being saved:</h2>

      <ul>
      <% review_reply.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :review_id %>
    <%= form.text_field :review_id %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
